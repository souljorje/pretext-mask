{
  "name": "pretext-mask",
  "version": "1.0.0",
  "description": "Seeded SVG glyph mask generator powered by Pretext text measurement.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "smoke": "PLAYWRIGHT_BROWSERS_PATH=.ms-playwright playwright test"
  },
  "keywords": [
    "masks",
    "glyphs",
    "svg",
    "pretext"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@chenglou/pretext": "^0.0.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.5"
  }
}
